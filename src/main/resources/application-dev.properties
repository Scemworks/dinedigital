# In-memory H2 for fast local dev
spring.datasource.url=jdbc:h2:mem:dinedigital;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=none

# Initialize schema
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# Dev JWT secret (>=32 bytes). For production, set SECURITY_JWT_SECRET env var or application-prod.properties.
security.jwt.secret=base64:c2VjdXJlLWRldi1qd3Qtc2VjcmV0LTI1Ni1iaXRzLXNob3VsZC1iZS1sb25nLWVub3VnaA==

# Log to file (relative to app working dir)
logging.file.name=app.log
